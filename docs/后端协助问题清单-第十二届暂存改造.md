# 后端协助问题清单 —— 第十二届报名暂存改造

> **作者**：前端负责人　**日期**：2026-09-23　**对应前端分支**：`jy`
> **本文档由前端单方面审计得出**，后端代码一行未改（`git status --short` 为空，可核）。

---

## 【2026-09-23 状态更新 —— 先读这一段再看下文】

下面「一、结论速览」那张表是**审计当时**的状态，已经**大面积过期**。
后端在同日 `07056da`（分支 `jy`）里修掉了其中绝大多数，该提交动了 8 个文件
（含迁移 `0006_reportdraft_unique_editing.py` 与 84 行回归测试）：

```
apps/api/registration_form.py                      | 15 ++--   → P0-4
apps/api/views.py                                  | 69 ++++-- → P0-3 / P1-2 / P2-2 / P2-4
apps/core/migrations/0006_reportdraft_unique_editing.py | 49 +  → P2-1
apps/core/models.py                                | 15 ++++
apps/core/report_drafts.py                         | 82 ++++--  → P0-1 / P0-2 / P0-3 / P1-1
apps/core/services.py                              | 11 ++-
tests/test_export_form.py                          |  7 +-
tests/test_report_drafts.py                        | 84 ++++++  → P2-3（部分）
```

| 编号 | 现状 | 说明 |
|---|---|---|
| P0-1 新增页覆盖被驳回报名 | ✅ 已修 | `create_or_get_draft` 加 `report_id__isnull=True` |
| P0-2 300 字简介 400 | ✅ 已修 | 引入 `_FIELD_LIMITS`，`desc` 单独放到 1000 |
| P0-3 同校第二条正式报名 | ✅ 已修 | 三入口统一 `lock_user_slot` + `assert_report_quota` |
| P0-4 导出表长号并进「其他」 | ✅ 已修 | `FORM_INSTRUMENTS = INSTRUMENTS`，两侧同源 |
| **P1-1 重进 edit-draft 冲掉暂存** | ✅ **已修** | 已有 `STATE_EDITING` 草稿时直接返回，不再重播 |
| P1-2 `/export/data` 跨校泄漏 | ⚠️ 半修 | 已加 `filter(user_id=request.auth.id)`，**越权读取已堵住**；但该接口对 committee 仍不返回全量（全量在 `/admin/export/*`，而那里 `role_error(request, 3)` 只放行 type==3），**committee 全量导出的可用性另算** |
| P1-3 统计页与报名表口径不一致 | ❌ **未动** | `07056da` 之后没有任何提交碰过统计口径 |
| P1-4 用餐预约功能断开 | ❌ **未动** | 后续 `2f899c6` 只调了「用餐填报」的行高与对齐，是样式，不是这条 |
| P2-1 草稿无唯一约束 | ✅ 已修 | 迁移 `0006`：先 `RunPython` 去重，再加两条部分唯一约束 |
| P2-2 非对象 JSON 回 500 | ✅ 已修 | `parse_body` 新增 `BodyError`，异常处理器回 400 |
| P2-3 edit-draft / 驳回重提零覆盖 | 🟡 部分 | 新增 6 条回归，但 `edit-draft` 与重提链路仍无专门用例 |
| P2-4 草稿接口不写 `write_log` | ✅ 已修 | create / submit / edit-draft 三处补齐 |
| P2-5 sqlite 下 `select_for_update` 空操作 | ⚪ 提示项 | 不是缺陷，本地测不出来这一条依然成立 |

**⚠️ P1-1 已修的连带影响**：编辑页「不显示暂存按钮」的技术理由随之失效。
但按钮**仍然不放回去** —— 那是产品要求（「报名修改」页只留提交），
与后端修没修无关。详见 `docs/报名暂存实施报告.md` 与 `OrchestraForm.vue` 的注释第 5 点。

**本次前端另修的一处（不在本清单内）**：`assert_report_quota` 原先只按 `user_id` 计数、
不看 `group`，导致同一所学校报完中学组就再也报不了小学组 —— 与 `HaveToRead.vue`
承诺的「小学组、中学组可各报一支（最多两支）」对不上。已在后端 `1d50aec` 按组别计数修复。

---

## 零、怎么读这份文档

### 与既有两份文档的关系（避免重复劳动）

| 文档 | 状态 |
|---|---|
| `BACKEND_ISSUES.md` | 仍然有效，其 **BE-02** 在本文档里有**新的、更严重的证据**（见 P0-3） |
| `docs/草稿接口联调问题反馈.md` | 其中的「问题 1～5」**均已由后端修复**，本文档不再重复；但它末尾「§十 三条必须确认的点」里的**第 1 条已经过期**，见下方「附：一处会误导后端的过时描述」 |
| **本文档** | 本轮新发现的问题，以及前端**无法自行修复**、必须由后端处理的部分 |

### 每一条都附「怎么自己复现」

所有结论都来自**实际执行**，不是读代码推测。探针文件在仓库之外的临时目录
（`%TEMP%\ylb-probe\`），通过 `PYTHONPATH` 挂进来跑，**不往 yilinbeihou 仓库里放任何文件**：

```bash
cd /d/zhuomian/yilinbei/yilinbeihou
PYTHONIOENCODING=utf-8 PYTHONPATH="C:/Users/Lenovo/AppData/Local/Temp/ylb-probe" \
  DB_ENGINE=django.db.backends.sqlite3 DB_NAME=:memory: \
  .venv/Scripts/python.exe manage.py test probe_rejected probe_more probe_export probe_json -v 0
```

每条下面的「证据」都写明了它出自哪个探针的哪一号用例，可单独跑。

### 等级口径

沿用审计时的定义：**P0** = 数据错误 / 重复报名 / 数据覆盖 / 无法提交；
**P1** = 核心功能异常 / 暂存丢失 / 并发覆盖 / 驳回无法重新提交；
**P2** = 普通功能异常。

---

## 一、结论速览

| 编号 | 等级 | 一句话 | 证据来源 | 前端能否自己修 |
|---|---|---|---|---|
| **P0-1** | 🔴 P0 | 「新增报名」页会把内容写进**被驳回的那份正式报名**，原报名被静默改写 | `probe_rejected` test_10 | ❌ 后端 `create_or_get_draft` |
| **P0-2** | 🔴 P0 | 乐团简介超过 **255 字**（红头文件允许 300 字）→ 暂存和提交**全部 400**，一个字段卡死整张表 | `probe_more` 11 / 11b | ❌ 后端 `_string` 默认上限 |
| **P0-3** | 🔴 P0 | 同一学校**能提交出第二条正式报名**，草稿链路连省级 8 队限制都绕过了 | `probe_rejected` test_09 | ❌ 需数据库约束 |
| **P0-4** | 🔴 P0 | 导出的**官方附件2**里，**长号被并进「其他」栏**（且栏目次序错位）——打出来盖章的那张表与红头文件不符 | 源码核对 `registration_form.py:12-17` | ❌ 后端乐器表漏项 |
| ~~**P1-1**~~ | ~~🟠 P1~~ | ~~重复点「修改报名」→ 用户**已暂存的修改被正式报名内容冲掉**~~　→ **已于 `07056da` 修复**，见文首状态更新 | `probe_rejected` test_06 | ~~⚠️ 前端已做本地镜像兜底，服务端仍是丢的~~ |
| **P1-2** | 🟠 P1 | `GET /api/export/data` **无角色校验、无用户过滤**，任何学校账号都能导出**全市所有学校**的报名数据 | `probe_export` 12r | ⚠️ 前端可先隐藏入口，根因必须后端修 |
| **P1-3** | 🟠 P1 | 统计页的「总数」与导出的报名表**口径不一致**，两个数字对不上 | 源码核对 | ❌ 后端口径 |
| **P1-4** | 🟠 P1 | **用餐预约整条功能是断的**：界面无控件、后端只画 `√` 不记人数（红头文件要求写人数）、格子数 6 与红头文件 4 不符 | 源码核对 + 附件2 原文 | ❌ 见下方「为什么前端不擅自补控件」 |
| **P2-1** | 🟡 P2 | `report_draft` 表无唯一约束，并发下可产生两条「编辑中」草稿 | 源码核对 | ❌ |
| **P2-2** | 🟡 P2 | 请求体是合法 JSON 但**不是对象**（如 `"abc"`）→ **500**，不是 400 | `probe_json` | ❌ |
| **P2-3** | 🟡 P2 | `edit-draft` / 驳回重提**零测试覆盖**——本轮 4 个最严重的缺陷全在无人看守的区域 | `tests/test_report_drafts.py` 仅 6 例 | ❌ |
| **P2-4** | 🟡 P2 | 草稿接口不写 `write_log`，出问题查不到人 | 源码核对 | ❌ |
| **P2-5** | 🟢 提示 | 本地 sqlite 下 `select_for_update()` 是**空操作**，并发问题在本地测不出来 | 源码核对 | ❌ |

**前端本轮已修、不需要后端配合的部分**见第五节。

---

## 二、P0：必须优先修

### P0-1　新增页会把内容写进被驳回的正式报名（数据覆盖）

**症状**：某校有一份被驳回的报名 R1。用户点「修改报名」进入编辑（此时后端建了一条
`report_id = R1` 的草稿），随后又切去「赛事报名」新增页填一份全新的表。结局是：

- 新增页填的内容**没有变成新报名**；
- 被驳回的 **R1 被整份替换**成新表的内容；
- `Report` 表条数仍然是 1，**从条数上完全看不出问题**。

**根因**：`apps/core/report_drafts.py:229` `create_or_get_draft()` 的筛选条件是

```python
draft = ReportDraft.objects.select_for_update().filter(
    user_id=user.id, scope=scope, state=ReportDraft.STATE_EDITING   # ← 没有 report_id
).order_by("id").first()
if draft is None:
    return ReportDraft.objects.create(...)
draft.payload = payload        # ← 无条件覆盖
```

它按用户 + 渠道找「唯一一条编辑中的草稿」，**完全不看 `report_id`**。而「新增报名」
和「修改被驳回的报名」是两种业务意图，却共用同一条草稿行。

**证据**（`probe_rejected` test_10，实际输出）：

```
OK   edit-draft 草稿绑定 R1
FAIL 新增页拿到的是新草稿（report_id 应为空）   期望 None  实际 '1'
FAIL 新增页没有复用编辑中的草稿                 期望 True  实际 False
FAIL R1 的内容未被新增页覆盖                    期望 True  实际 False
     （Report 数=1，R1 名称='这其实是一份全新的报名'，提交返回 report_id=1）
```

**建议改法**（二选一，推荐第 1 种）：

1. `create_or_get_draft` 增加 `report_id` 维度：新增页只认 `report_id IS NULL` 的草稿，
   `edit-draft` 只认 `report_id = R` 的草稿。两条业务线彻底分开。
2. 若要保持「一个用户同时只有一条编辑中草稿」，则新增页在发现已有草稿**绑定了
   `report_id`** 时必须报错，而不是静默覆盖。

> 附带提醒：`ReportDraft.report_id` 已经有索引（`models.py:233`），按它查询不会有性能问题。

---

### P0-2　乐团简介 255 字上限卡死整条报名链路（无法提交）

**症状**：用户在「乐团简介」里写 256～300 字 → 点暂存 → 弹「desc 长度不能超过 255」；
点提交同样失败。**用户没有任何办法继续**，只能删字。

**三个数字互相对不上**：

| 位置 | 文件:行 | 值 |
|---|---|---|
| 红头文件（两处原文「300字以内乐团简介」） | — | **300** |
| 前端输入框 | `yl-frontend/src/components/elementary/OrchestraForm.vue:219` | `maxlength="300"` |
| 数据库列 | `yilinbeihou/apps/core/models.py:178` | `max_length=1000` |
| **后端校验** | `yilinbeihou/apps/core/report_drafts.py:88` `def _string(value, field, required=False, maximum=255)` | **255** ← 卡在这里 |

`:159` 那一行调 `_string(value, field)` 时**没有传 `maximum`**，于是一律落到默认的 255。

**证据**（`probe_more` 11 / 11b，实际输出）：

```
【11】255 字 → 200
  FAIL 256 字 → 应当也能存下      期望 200  实际 400（msg='desc 长度不能超过 255'）
  FAIL 300 字（前端上限）→ 应当能存下   实际 400
【11b】首次暂存 HTTP 期望 200 实际 400  ⇒ 草稿根本没建起来（Report 数=0）
```

**建议改法**：给 `desc` 单独上限，与 DB 列对齐：

```python
_LIMITS = {"desc": 1000}          # 与 Report.desc max_length 对齐
...
result[field] = _string(value, field, maximum=_LIMITS.get(field, 255))
```

**前端为什么不能自己修**：把 `maxlength` 改成 255 能消除报错，但那等于**前端擅自把
红头文件的 300 字砍成 255**，是拿产品规则去迁就实现缺陷。前端保留 300 不动。

---

### P0-3　同一学校可产生第二条正式报名（重复报名）

**症状**：某校已提交报名 R1，用户重新进入报名页再填一份并提交 → 库里出现 R2。
违反红头文件「每所学校限报一支队伍，且只能参加一个组别」。
（**注**：该句口径已于 2026-09-23 放宽为「每所学校每个组别各一支」，见本节下方的
【2026-09-23 口径变更】——本条的**症状与根因不受影响**，后端一条约束都没有这件事没变，
变的只是额度上限该怎么算。）

**根因**：走草稿链路的提交最终调用 `apps/core/report_drafts.py:206`
`create_report_from_submission()`，它的全部内容就是：

```python
def create_report_from_submission(user, submission, scope=None):
    ok, stored = store_people(user, list(submission.people))
    if not ok:
        raise InvalidSubmission(stored)
    report = Report.objects.create(user_id=user.id, **submission.report)   # ← 无任何数量校验
    attach_report_people(report.id, stored)
    return report
```

对比老的直建路径 `apps/api/views.py:109` `create_report()`，那里**至少有**省级的 8 队限制：

```python
if province and Report.objects.filter(user_id=user.id).count() >= 8:
    return response(failure("目前您的单位已超报送限制,无法再继续进行报送!"))
```

也就是说：**新的草稿提交路径比老的直建路径更宽松**，把仅有的那道限制也绕过去了。

**证据**（`probe_rejected` test_09）：

```
OK   第二次 POST 草稿 HTTP
OK   新草稿未关联原报名
OK   第二次提交 HTTP
FAIL Report 表仍只有 1 条      期望 1  实际 2
     （实际 Report 数=2；第二条 report_id=2，首条=1）
```

**这与 `BACKEND_ISSUES.md` 的 BE-02 是同一条**，但 BE-02 描述的是 `create_report`，
**没有覆盖草稿提交路径**。请按本文档补充处理。

**建议改法**：`create_report_from_submission` 提交前检查
`Report.objects.filter(user_id=user.id, deleted_at__isnull=True).exists()`，
命中则返回 409 + 明确文案。**最终仍建议在数据库层加唯一约束**（并发下应用层检查有竞态）。

> ⚠️ 本节的**额度口径已于 2026-09-23 放宽**，上面那句建议改法（`exists()` 一刀切）
> 不再适用。请以下面的【口径变更】为准，实现以 `07056da` 落地的
> `lock_user_slot` + `assert_report_quota` 为基础修改即可。

#### 【2026-09-23 口径变更】限报从「每校一支」改为「每校每个组别各一支」

**新口径**（组委会决定，**已不是红头文件正文一(二)的原文**）：

> 每所学校**每个组别**限报一支队伍。小学组、中学组可**各报一支（最多两支）**，
> 大学组限报一支。

背景：有部分学校同时设小学和中学，本就应当能各报一支；原来的
「且只能参加一个组别」把这批学校挡掉了。

**为什么落到后端是三个 scope 分开看**（渠道由 `user.type` 决定，一个账号只有一个渠道）：

| scope | 对应账号 | 该渠道可选的 `group` | 新额度 |
| --- | --- | --- | --- |
| 0 | 学校（高校渠道 `/school/*`） | 只有「大学组」 | 按组别 ≤1 —— **与现在行为等价，不用改** |
| 1 | 市级（中小学渠道 `/city/*`） | 「小学组」「中学组」 | 按组别 ≤1 —— **合计最多 2，本次放宽的是它** |
| 4 | 省级 | — | 维持总数 ≤8 |

**具体改动**：`apps/core/report_drafts.py:87` 现在是平铺计数，缺一个组别维度：

```python
REPORT_QUOTA_BY_SCOPE = {0: 1, 1: 1, 4: 8}
DEFAULT_REPORT_QUOTA = 1

def assert_report_quota(user, scope):
    limit = REPORT_QUOTA_BY_SCOPE.get(scope, DEFAULT_REPORT_QUOTA)
    current = Report.objects.filter(user_id=user.id).count()      # ← 没有 group
    if current >= limit:
        ...
```

需要签名加上 `group`，并把非省级的计数改成按组别。**两个调用点都要传**：

- `apps/api/views.py:126`（`create_report`）—— 取 `data.get("group")`
- `apps/core/report_drafts.py:249`（`create_report_from_submission`）——
  取 `submission.report.get("group")`

`lock_user_slot()` 的调用顺序不用动，本次只多一个维度、不加新的竞态。

**⚠️ 顺带必须补的一件事：`group` 现在是没有白名单的自由字符串。**

`_string()`（`report_drafts.py:122-132`）只做 `strip()` 和长度校验，**不校验取值**。
`group` 又恰好是提交时的必填项（`:203`），所以客户端可以发任意字符串。
一旦**拿它当额度键**，下面这些就各开一个新桶，限报被整个绕过：

```
"小学组A"      "小学組"（繁体）      "小学 组"      "小学组 "（已被 strip 挡掉）
```

建议在 `parse_submission_payload` 里把 `group` 收敛成枚举
（`{"小学组", "中学组", "大学组"}`），不在其中一律 400。
这条不做的话，新额度形同虚设 —— 它现在就是这个系统里唯一的重复报名防线。

> 佐证这不是杞人忧天：`Report.group` 是无 choices 的 `CharField`，而委员会各页
> 过滤它时已经在传**不一致的值**（`CommitteeReportList.vue` 的注释记载
> elementary 传数字 `0`、elementary1 传字符串 `"小学组"`）。既然存量里可能同时存在
> `0` 和 `"小学组"`，按组别计数就更不能依赖「值一定是那三个中文串」。

**文案要跟着改**：`assert_report_quota` 现在这句

```python
message = ("每所学校限报一支队伍，您已有报名记录" if limit == 1
           else "目前您的单位已超报送限制,无法再继续进行报送!")
```

在新口径下**是错的**（一所中小学本来就可以有两支）。改为点名组别，例如
`"该组别（%s）已有报名，每所学校每个组别限报一支队伍" % group`。
这句 `msg` 前端会**原样展示给用户**，是用户唯一能知道真实原因的渠道。

**前端已同步完成**（本轮，未提交）：

- `src/components/common/HaveToRead.vue` —— 「报名须知」抽屉里那句已按新口径改写，
  并在注释里标注了「此处**故意**与红头文件不一致，不要照文件改回去」。
- `src/components/elementary/OrchestraForm.vue` —— 额度错误的提示文案改为
  「该组别的报名是「已驳回」状态…」并**在有多个组别可选的渠道**追加一句
  「若您要报的是另一个组别，可直接新增报名」（高校渠道只有一个组别，不显示这句）。
- `src/api/reportDraft.js` —— 新增 `DRAFT_ERR.QUOTA` 种类。之前
  `REPORT_QUOTA_EXCEEDED`（挂在 **409** 上）不在码表里，会一路掉进
  `status === 409` 被误判成**版本冲突**，用户看到的是硬编码的
  「该草稿已在其他页面或设备更新」+ 一个永远无效的「重新加载服务器草稿」按钮，
  后端给的真实原因一个字都不显示。已修。

**验收**（后端改完用这三条自测）：

- [ ] 中小学渠道账号：提交「小学组」成功后，**再提交「中学组」应当放行**（这是本次放宽的核心）
- [ ] 中小学渠道账号：已有「小学组」时**再提交「小学组」应当被拒**，且 `msg` 点名「小学组」
- [ ] 高校渠道账号：行为与改前一致 —— 只能有一支「大学组」
- [ ] `group` 传 `"小学组A"` 这类值应当 **400**，而不是被当成一个新组别放行

---

### P0-4　导出的官方附件2：**长号被并进「其他」栏**，栏目次序也与红头文件不符

> 这一条与暂存改造无关，是**导出功能本来就有**的缺陷。放在 P0 是因为它污染的是
> **那张要打印、盖章、扫描回传的官方报名表**——附件2 是报名的法定凭证。

**红头文件原文（两处，均逐字核对）**：

- 三、报名要求（二）乐团要求：
  > 管乐团应包含铜管（含圆号、小号、**长号**、次中音号和大号等）、木管（含长笛、单簧管、萨克斯管等）以及打击乐器……
- 附件2「正式队员名单」栏，**长号有独立栏目**，且次序固定：

  > 短笛：　长笛：　单簧管：　低音单簧管：　中音萨克斯：　次中音萨克斯：
  > **上低音萨克斯：　双簧管：　大管：　小号：　长号：　圆号：　上低音号：　大号：　打击乐：　低音大提琴：　其他：**

**后端两个乐器表，一个对、一个错**：

| 位置 | 内容 | 项数 | 与附件2 比对 |
|---|---|---|---|
| `apps/api/export_services.py:49-53` `INSTRUMENTS` | 短笛…**长号**…其他 | **17** | ✅ **逐字逐序完全一致** |
| `apps/api/registration_form.py:12-17` `FORM_INSTRUMENTS` | 短笛…小号、**上低音萨克斯**、圆号…其他 | **16** | ❌ **缺「长号」**；且**「上低音萨克斯」位置错**（被排到「小号」之后，红头文件里它在「次中音萨克斯」之后） |

`registration_form.py` 的模块 docstring 与 `:110` 的注释**把这个漏项写成了既定口径**：

```python
# 模块 docstring：乐器名经 _instrument_bucket 归一到官方表格的 16 个栏目
# :12  # 官方表格的 16 个乐器栏目（含上低音萨克斯、无长号；表外乐器并入"其他"）
FORM_INSTRUMENTS = (...)                      # 16 项，含上低音萨克斯、无长号
# :110
bucket = "其他"  # 表外乐器（如长号）统一并入"其他"栏
```

**后果链**（`form_context()` → `_instrument_bucket()` → `:110` 兜底）：

1. 报名库里 `Person.instrument` 存的是 `"长号"`（前端下拉确有长号，见下）；
2. `_instrument_bucket("长号")` 返回 `"长号"`，但 `instrument_names` 这个 dict
   **是按 `FORM_INSTRUMENTS` 建的，没有「长号」这个键**；
3. 于是 `:110` 的 `bucket = "其他"` 兜底生效 → **这名长号队员被印在「其他」栏下**。

**为什么这是 P0 而不是 UI 问题**：`_instrument_bucket` 只做「归一」，判据是**乐器名是否
在官方栏目里**；长号**在**官方栏目里，是被后端自己漏掉的。所以这不是「表外乐器归到其他」
的正常兜底，而是**把表内乐器当成表外**——3 名长号学生在一张盖章上传的报名表上被登记为
「其他乐器」。组委会按表核对编制时会直接读到错误信息。

**前端侧是对的，不要动**：
`yl-frontend/src/components/elementary/PersonTable.vue:124-141` 的 17 个 `el-option`
**逐字、逐序**与附件2 一致（含长号，长号在「小号」之后）。前端下拉选得出长号，
后端打印不出来——两侧对同一条规则给出了不同答案，**以后端为准去改前端会让错的那一侧变成两处**。

**建议改法**：把 `FORM_INSTRUMENTS` 与 `export_services.INSTRUMENTS` **合并成一处常量**
（两处逐字相同后自然就对了），或直接令 `FORM_INSTRUMENTS = INSTRUMENTS`。顺序按附件2：

```python
FORM_INSTRUMENTS = (
    "短笛", "长笛", "单簧管", "低音单簧管", "中音萨克斯", "次中音萨克斯",
    "上低音萨克斯", "双簧管", "大管", "小号", "长号", "圆号", "上低音号", "大号",
    "打击乐", "低音大提琴", "其他",
)   # 17 项，与 export_services.INSTRUMENTS 逐字一致
```

改完后 `:110` 的 `"其他"` 兜底**应当保留**——它对真正的表外乐器（如「次中音号」，
红头文件（二）里用的是这个词，而附件2 栏目写的是「上低音号」）仍是必要的。
只需在测试里断言 `_instrument_bucket("长号") == "长号"`，防止再次被误并。

---

## 三、P1：核心功能异常

### P1-1　重复进入「修改报名」会冲掉用户已暂存的修改

> **✅【2026-09-23 已修 · `07056da`】** 后端已在 `_edit_draft()` 里加了这道判据
> （现位于 `apps/api/views.py:875-881`）：查出绑定在该 report 上的草稿后，
> 若 `state == STATE_EDITING` 就**直接返回它**，不再用 `build_payload_from_report` 重建；
> 只有「还没有草稿」和「已提交又被再次驳回」（`:882-896`，保留了正确语义：清掉提交痕迹重新播种）
> 两条路才重建。与下方「建议改法」完全一致。
>
> 因此**本节以下内容转为历史记录**，不要按它再改一遍。
> 两条连带影响：
> 1. 编辑页「暂存」按钮**仍不恢复** —— 撤掉它现在的依据是产品要求，与后端修没修无关；
> 2. 「前端本地镜像只是兜底」这句话的最强理由已消失，但本地镜像本身的必要性没有变：
>    编辑页没有写服务端草稿的入口，改了不提交时服务端那份始终是旧的。

**症状**：用户被驳回后点「修改报名」，改到一半（已点过「暂存」），中途切走再点一次
「修改报名」→ 之前改的内容**全没了**，表单变回正式报名里的旧内容。

**根因**：`apps/api/views.py:871` `_edit_draft()` 每次进入都**无条件**重建草稿：

```python
draft = ReportDraft.objects.select_for_update().filter(
    report_id=report.id, user_id=request.auth.id, scope=scope).first()
payload = encode_payload(build_payload_from_report(report))   # ← 每次都从正式报名重建
if draft is None:
    draft = ReportDraft.objects.create(..., payload=payload, ...)
else:
    draft.payload = payload                                   # ← 覆盖掉用户改的内容
    draft.version = F("version") + 1
```

**证据**（`probe_rejected` test_06，实际输出）：

```
FAIL 第二次 edit-draft 后内容仍是用户改的
     期望 '用户改了一半'   实际 '探针曲目'
     依据 重复点击「修改报名」不该用旧报名内容覆盖用户已暂存的编辑
OK   version 只增不减
```

**建议改法**：`_edit_draft` 找到**已存在且 `state=EDITING`** 的草稿时，**直接返回它**，
不要用 `build_payload_from_report` 重建。只在草稿不存在（或已提交）时才从正式报名播种。

> 前端已做的兜底：编辑页有本地镜像（`localStorage`，**不联网**），在 `beforeunload`
> 与路由切走时落盘，重进页面时把用户最后看到的内容盖回去。
> （【2026-09-23 修订】dist 原有的「每 60 秒写一次」定时器已按产品要求移除，现在只在
> 离开那一刻写一次；原句写的「60 秒本地镜像」不再成立。）
> **这只是兜底**——它救不了换设备、清缓存、清 localStorage 的场景。
> （订正：末句原文还有「而且服务端那份确实已经被冲掉了」—— 这一条已随 `07056da` 失效。）

> 【2026-09-23 追加，同日再订正】**编辑页的「暂存」按钮已从前端移除**
> （`OrchestraForm.vue`，判据 `cfg.mode === 'create'`），状态文案一并移除。
>
> 追加时给的理由是「本缺陷存在 → 那个按钮给的是假保证」。**该理由已随 `07056da` 失效**，
> 现在撤掉的依据**只剩产品要求**：被驳回的文件点「编辑」进入的「报名修改」页不需要暂存按键。
> 所以 **P1-1 修完也不要把按钮放回去** —— 那不是订正，是回退需求。
> 编辑页的提交路径未受影响：提交时仍按 §十 强制暂存一次再提交。

---

### P1-2　`GET /api/export/data` 跨校数据泄漏（建议与 P0 同批修）

**症状**：**任何学校账号**（`type=0`）调用该接口，都能拿到**全市所有学校**的报名数据。

**根因**：`apps/api/views.py:394`

```python
@api.get("/export/data", auth=auth)
def export_data(request):
    qs = Report.objects.all().order_by("id")        # ← 没有 user 过滤
    if request.GET.get("group"):                    # ← group 是组别过滤，不是归属过滤
        qs = qs.filter(group=request.GET["group"])
    write_log(request.auth, 6, "导出报送数据")
    return reports_export_response(qs, "数据导出.xlsx")
```

对比同文件的 `/export/report`（`:376`）与 `/export/person`（`:385`），它们都写了
`filter(user_id=request.auth.id, ...)`；`/export/data` 既没有这个过滤，**也没有 `role_error()`**。

**证据**（`probe_export` 12r，含正向对照，排除「解析写错了」的可能）：

```
OK   HTTP 200（校级账号可访问）        （格式=csv，500 字节）
OK   能看到自己学校的数据               ← 正向对照，证明解析方式没错
FAIL 看不到别校数据      期望 False  实际 True
OK   组委会账号（type=2）能看到学校的报名
FAIL 带 group 过滤时依然不该看到别校数据
```

> 探针里是 csv 而不是 xlsx，是因为本地 `.venv` 未装 `openpyxl`，走了
> `export_services.py:339` 的 `except ImportError:` 兜底分支。
> 生产环境 `requirements.txt:7` 声明了 `openpyxl==3.1.5`，导出的是真 xlsx——
> **但这不影响结论**，泄漏发生在查询集构造阶段，与导出格式无关。

**按等级表这属于「权限」，列在 P1；但它是一次数据安全事故，建议与 P0 同批修。**

**建议改法**：与 `/export/report` 对齐——`qs = qs.filter(user_id=request.auth.id)`，
并补上 `role_error(request, expected)`；组委会/管理员需要全量导出的话，
应另开一个明确鉴权的管理端接口。

---

### P1-3　统计页数字与导出表口径不一致

同一个用户看到的「总数」和导出出来的报名表**条数对不上**，因为两处对**状态**的处理不同：

| 位置 | 代码 | 口径 |
|---|---|---|
| `views.py:700` / `:704` / `:709`（`scoped_total`，即各首页统计） | `qs.count()` | **全部状态**，含已驳回(-1)、未填写(-2) |
| `views.py:196`（`stats`） | `qs.count()` | **全部状态** |
| `views.py:376` / `:385`（`/export/report`、`/export/person`） | `filter(status__gte=0)` | **排除**已驳回(-1)、未填写(-2) |

**症状**：某校有 1 条正常 + 1 条被驳回，首页显示「总数 2」，导出的报名表只有 1 页。
用户会认为导出漏了数据。

**证据**：源码逐行核对（上表每格都标了文件:行）。这一条没有写成探针，因为它是纯粹的
口径定义问题，不是行为异常——**修之前需要先确定哪个口径是对的**。

**建议**：请后端明确「各首页展示的『总数』是否应当含已驳回/未填写」，
并把 `scoped_total` / `stats` / 两个 export 的过滤条件统一。前端会跟着改文案。

---

### P1-4　用餐预约：**整条功能是断的**（前端无控件 / 后端不记人数 / 格数与红头文件不符）

> 这一条同样是**导出/表单本身的缺陷**，不是暂存改造引入的。列在 P1 是因为它不阻塞提交，
> 但红头文件明确要求的功能在系统里**完全不可用**。

**红头文件附件2 原文**（用餐预约栏 + 其下的备注 1）：

> 用餐预约
> 　11月21日午餐　11月21日晚餐　11月22日午餐　11月22日晚餐
> 备注：1. 如果需在成都理工大学食堂购票用餐，**请备注时间并在对应位置写上就餐人数**。

三处问题，逐条列清：

**（1）格子数：后端 6 格，红头文件 4 格**

```python
# apps/api/registration_form.py:19-20
MEALS = ("11月20日午餐", "11月20日晚餐", "11月21日午餐", "11月21日晚餐",
         "11月22日午餐", "11月22日晚餐")
MEAL_HINTS = (("20", "午"), ("20", "晚"), ("21", "午"), ("21", "晚"),
              ("22", "午"), ("22", "晚"))
```

红头文件里**没有 11月20日的用餐格**。多出的两列会让打印出来的报名表
与官方空白表式样不一致；学校若在 20 日两格填了，组委会也没有对应的安排可兑现。

> 【更正】本文件此前在这段里写过「20 日是报到日，组委会不安排食堂购票用餐」，
> 并把「报到时间」误引作「上报时间」。**那半句是推测，不是文件内容**，已删除。
> 正文只写了「报到时间：2026年11月20日8:00—12:00」与「展示时间：2026年11月20日至22日」，
> 并未说明用餐栏为何不含 20 日。结论不受影响 —— 表格样式以附件2 为准，
> 文件里是 4 格，导出就应当是 4 格。

**（2）后端只画 `√`，红头文件要的是「就餐人数」**

```python
# apps/api/registration_form.py:56-79  _meal_cells()
if text.isdigit() and 0 <= int(text) < len(MEALS):
    cells[int(text)] = "√"        # ← 无论传进来什么，格子里只会出现 √
```

`dinner_reservation` 被当作**「选了/没选」的布尔集合**，而红头文件要求填的是**人数**。
更麻烦的是它的匹配是**宽松包含**：

```python
if text == label or all(part in text for part in MEAL_HINTS[index]):
```

**实测**（直接调 `_meal_cells`，按 `registration_form.py:17` 的现有 6 格口径）：

| 传入 | 实际结果 | 对不对 |
|---|---|---|
| `["11月21日午餐"]` | `cells[2] = "√"` | ✅ 只有「逐字完整文案」这一种是对的 |
| `["11月21日午餐20人"]` | `cells[0] = "√"` | ❌ **落进了 11月20日午餐**，人数也丢了 |
| `[{"meal":"11月21日午餐","count":20}]` | `cells[0] = "√"` | ❌ 同上 |
| `["2"]` | `cells[2] = "√"` | ❌ 2 被当**下标**，不是人数 |
| `["10月1日午宴"]` | 全空，整条进备注 | — |

第二行是这套匹配最坏的地方：`"11月21日午餐20人"` 里**「20人」的那个 20** 正好命中
`MEAL_HINTS[0] = ("20", "午")`，而循环**命中即 break**，于是这条记录被记到
**11月20日午餐**上，真正的 21 日那格反而空着。
**人数被丢 + 落到错格**，而导出的表格上两格都「看起来正常」。

> 【更正】本文件此前在这一段写的是「`"11月21日午餐2人"` 命中并写 `√`」，
> 没说清落进哪一格，读起来像是落进 21 日那格。**实际落进 20 日**，已按实测改写。

想保留人数只能故意传入匹配不上的字符串，让它落进 `leftovers` 再由
`form_context()` 追加到备注栏——**靠兜底路径承载正常语义，是脆的**：
这套匹配本来就宽松到会把「20人」认成「20日」，指望它稳定地放行某类字符串不现实。

**（3）前端模板里根本没有这个控件**

`OrchestraForm.vue:395` 已注明：`dinner_reservation:[]` 在表单初值里，但**模板中没有任何控件绑定它**。
`src/` 全目录 grep `dinner_reservation` 只有 8 处，全部是：dist 初值、类型注释、
`draftPayload` 的往返透传——**没有一处是 UI**。

也就是说：一所学校需要 11月21日午餐 20 份，**在系统里没有任何地方可以填**。

---

**⚠️ 给后端的接口陷阱（改前端之前必须先定这个）**

`_meal_cells` 同时接受三种写法，**下标语义绑死在 `MEALS` 的 6 格顺序上**：

| 写法 | 例 | 落到哪一格 |
|---|---|---|
| 下标 `0-5` | `"0"` | `MEALS[0]` = **11月20日午餐** |
| 完整文案 | `"11月21日午餐"` | 命中同名格 |
| 简写 | `"21午"` | 由 `MEAL_HINTS` 定位 |

所以**如果前端按红头文件的 4 格做 UI、并发送下标 `0-3`，四顿饭会全部落到 11月20日**
（`0→20午`、`1→20晚`、`2→21午`、`3→21晚`）——**四顿全错，且表格上看起来完全正常**。
这是一个不会报错、只会静默错位的数据错误。

在 `MEALS` 改成 4 格（或改成「人数数组」）**之前，前端不得按 `0-3` 下标发送**。

**最终契约另见**：`yl-frontend/docs/用餐预约接口契约-第十二届.md`（**该文件为唯一事实来源**，
本节只描述问题，不重复契约）。契约里已定死线上格式为对象数组
`[{"meal": "11月21日午餐", "count": 20}]`，并写明了后端要改的四处
（`MEALS` 收敛为 4 格 / `_meal_cells` 写人数 / **`export_services.py:202` 的 Excel 导出**
/ 提交校验），以及存量数据的排查 SQL。

> 特别提醒后端**别漏了 Excel 导出那一处**：`/admin/export/data1` 现在是
> `"、".join(str(x) for x in dinner)`，格式一改就会把 Python 字典的 repr
> 打印进单元格。它和附件2 PDF 读的是同一列，必须一起改。

**为什么前端本轮不擅自把控件补上**（这是一次有意的取舍，不是漏做）：

1. 补了也只能发「选没选」，**红头文件要的人数照样丢**——等于做出一个**看起来能用、
   实际丢数据**的控件，比没有控件更坏（用户会以为填过了）。
2. 按 4 格发下标会把四顿饭静默错位到 20 日（见上表）。
3. 现在没有任何学校能用这个字段（模板里根本没有控件），**存量数据应当为零**
   —— 期望值，未核实生产库，排查 SQL 见契约文档第 4.4 节；
   等前端铺上控件、收到一批「只有 √ 没有人数」的数据之后再改，代价高得多。

所以这一条**故意留空**，等后端把契约定下来，前端一次性做对。

---

## 四、P2：健壮性与可维护性

### P2-1　`report_draft` 表无唯一约束，并发下会产生两条「编辑中」草稿

`apps/core/models.py:206-234` 的 `ReportDraft` 只有两个 `Index`，**没有任何
`unique_together` / `UniqueConstraint`**。而 `create_or_get_draft` 的
`select_for_update()` **锁不住一行还不存在的记录**（幻读）——两个并发 POST 会各自
`draft is None` 然后各建一条。

**建议**：加 `UniqueConstraint(fields=["user_id", "scope", "report_id"], ...)`
（`report_id` 为 NULL 的场景需按数据库类型确认 NULL 语义），或用
`get_or_create` + 重试。

### P2-2　请求体是合法 JSON 但不是对象 → 500 而非 400

`apps/core/services.py:27` `parse_body()` 对合法 JSON 原样返回，不校验类型。
于是 `POST /api/school/report/drafts` 带 body `"abc"` 时：

```
File "apps/api/views.py", line 762, in _draft_payload_body
    return data.get("payload", data)
AttributeError: 'str' object has no attribute 'get'
```

**证据**（`probe_json`，实际异常栈如上）：字符串 / 数组 / 数字三种 body 都触发。
生产环境（`DEBUG=False`）下这是一个 **500**，会污染错误日志并给客户端一个无意义的响应。

**建议**：`parse_body` 里加 `if not isinstance(value, dict): return request.POST.dict()`
或直接抛 400。

### P2-3　`edit-draft` / 驳回重提零测试覆盖

`tests/test_report_drafts.py` 共 **6 个用例**，覆盖：编解码、字段白名单、暂存期人员非必填、
版本冲突 + 跨用户隔离、提交幂等、scope 隔离。**没有任何一条覆盖 `edit-draft` 或驳回重提。**

本轮 4 个最严重的缺陷（P0-1、P0-3、P1-1，以及 P0-2 的 255 上限）**全部落在这一块无人看守的区域**。
建议把这 4 条按本文档的复现步骤直接固化成回归测试。

> **【2026-09-23 更新】** 这条建议**已被采纳**：`07056da` 往 `tests/test_report_drafts.py`
> 补了 6 条回归（用例总数 6 → 12），其中 4 条正是本节点名的 P0-1 / P0-2 / P0-3 / P1-1：
> `test_new_draft_does_not_touch_edit_draft`（P0-1）、
> `test_desc_over_1000_chars_is_rejected` + `test_long_desc_up_to_1000_chars_is_accepted`（P0-2）、
> `test_draft_submit_rejects_second_report_for_same_school`（P0-3）、
> `test_edit_draft_preserves_user_edits_on_second_entry`（P1-1）。
> 另加 `test_non_object_json_body_returns_400`（P2-2）。
>
> 但 **`edit-draft` 与「驳回重提」本身仍没有专门的用例** —— 上面这条 P1-1 的回归覆盖的是
> 「第二次进入不覆盖」，不是 edit-draft 的完整行为（驳回态校验、跨 scope、报告不存在等）。
> 建议保留这条 P2-3。

### P2-4　草稿接口不写 `write_log`

`:775` / `:790` / `:803` / `:815` / `:840` / `:861` / `:897` 这些草稿相关分支都没有
`write_log(...)`，而 `create_report`（`views.py:131`）、`export_report`（`:377`）等都有。
草稿是用户实际发生的数据写入，出问题时按日志查不到任何痕迹。

### P2-5　本地 sqlite 下 `select_for_update()` 是空操作

项目所有并发假设都建立在 `select_for_update()` 上，但 sqlite 不支持行级锁，
本地开发/测试环境**根本测不出并发问题**。这不是代码缺陷，是**验证盲区**——
建议后端在上线前用 PostgreSQL（或至少在生产同构环境）跑一次并发压测。

---

## 五、前端本轮已修完的部分（不需要后端配合）

列在这里是为了让后端知道**哪些情况已经不会发生了**，免得重复排查：

| 问题 | 修在哪 |
|---|---|
| 驳回重提后 `remark` / `establishment_name` 被清空 | `src/services/draftPayload.js:78` `ECHO_KEYS` — 读回来、原样送回去 |
| 人员行的 `major` / `other` / `remark` 被写成 NULL | `draftPayload.js` `personToPayload` / `payloadToPerson` |
| 登录页 `localStorage.clear()` 把草稿指针一起清掉 | `src/views/login/index.vue:114-125` — 改为只清登录态与内容缓存，保留 `draft_session:*` |
| 「检测已有草稿」恒返回空（信封解析错误） | `src/composables/useDraftSession.js:492` — 兼容 `data` 与 `data.data` 两种信封 |
| 编辑页无关闭页面落盘 | `OrchestraForm.vue` / `ProgramForm.vue` — 两个模式都接入 `beforeunload` + `onBeforeUnmount` 落盘（**不联网**；原先的 60 秒定时器已于 2026-09-23 按产品要求移除） |
| 提交按钮在保存中可重复点击 | `OrchestraForm.vue` 提交按钮 `:disabled` |
| 列表页快速翻页时旧响应覆盖新响应 | `ReportList.vue` — 递增请求序号，过期响应丢弃 |

> **另有 4 处按红头文件纠正 dist 的改动**（MOV 视频被误拒、铜管乐团看不到「指定曲目」、
> city 编辑页可选非法的「铜管乐团+大学组」、集体照提示文案拼错）——它们不是暂存改造的一部分，
> 单独列在 **6.2**，以免与上面的暂存类修复混在一起。

**验证**：前端 8 个探针套件全绿（人员字段 52 / 表单字段 32 / 重连 25 / 错误归一 8 /
会话 53 / 草稿 payload 42 / 人员编制 9 / 前后端契约），`npm run build` 通过。

其中「前后端契约」这一条跑的是**前端真实的 `buildDraftPayload()` 输出 → 后端真实的
`normalize_draft_payload()` / `parse_submission_payload()`**，可用来验证 P0-2 修好之后
300 字简介能正常通过。

---

## 六、红头文件符合性核对（逐条对表）

本节按红头文件《“意林杯”四川省第十二届管乐展示活动》正文 + 附件2 的**逐条要求**，
核对系统两侧的取值/文案/口径。**这一节是判断上面各条「该不该改」的依据**——
凡是与红头文件不符的，改法一律以文件为准，不拿实现去迁就。

核对所用原文取自随通知下发的 `系统需求.docx`（附件2 的栏目文字在其中逐字可见）。

### 6.1 前端已符合，后端无需改动

| 红头文件要求（原文出处） | 前端取值 | 结论 |
|---|---|---|
| 管乐团分为小学组、中学组和大学组；铜管乐团分为小学组和中学组（三·（一）） | 四个路由的 `establishmentOptions` × `groupOptions` 组合**穷举后全部合法**：city 新增/编辑 = 管乐团·铜管乐团 × 小学组·中学组；school 新增/编辑 = 管乐团 × 大学组 | ✅ **不存在「铜管乐团 + 大学组」这种非法组合** |
| 正式成员 35–65 / 预备 ≤5；铜管 20–45 / 预备 ≤3 / 打击乐 ≤8（三·（二）） | `src/config/personRules.js` `PERSON_RULES` 五个键，数字**逐个对上** | ✅ |
| 打击乐上限是「**其中**」——只数正式成员（三·（二）） | `validatePersonCount` 只在 `position===0` 时累加打击乐，预备不计 | ✅ 与后端 `members(0)` 口径一致 |
| 指挥不在正式队员名单内（附件2：指挥有独立栏目） | 指挥 `position===2` 只记账、不进任何上下限 | ✅ |
| 展示时长 小学12 / 中学15 / 大学18；铜管小学、中学均10（六·（三）） | `durationLimitSeconds` = 720/900/1080/600/600 | ✅ |
| 指定曲目和自选曲目**各一首**，管乐团与铜管乐团**都是**（六·（一）） | 四个变体**无条件渲染**「指定曲目」 | ✅ 前端已纠正 dist（详见 6.2） |
| **附件1 的 9 首指定曲目**，管乐 小学/中学/大学各二选一、铜管 小学·中学共用同一份三选一（附件1） | `OrchestraForm.vue` `NAME1_OPTIONS`：9 条曲名**逐字一致**（已按「在第一个『，作曲』或『，选自』处切断」的机械规则去掉出处署名），选项数 2/2/2/3 与「二选一/三选一」吻合，按 `establishment\|group` 组合取候选 | ✅ **无「铜管乐团 + 大学组」的候选**，与 6.1 第一行的组合收敛互相印证 |
| 视频格式 MP4 或 MOV，不超过 700MB（五） | `beforeUpload` 校验 `<700M` + `video/mp4`｜`video/quicktime` | ✅ 前端已纠正 dist（详见 6.2） |
| 乐团集体照 JPEG 或 TIFF，不低于 600dpi（四 / 附件2 备注2） | `beforeUpload1` 精确接受 `image/jpeg`｜`image/tiff` | ✅（600dpi 前端无法读，提示文案已写明，由后端保证） |
| 师生电子照片 JPG、每张不超过 100KB（四） | `PersonTable.beforeUpload`：`image/jpeg` + `<100KB` + 命名校验 | ✅ |
| 学生照片以身份证号后 6 位命名（四） | 同上，`PersonTable` 内做命名格式校验 | ✅ |
| 乐团简介 300 字以内（四 / 附件2 备注2） | `OrchestraForm.vue:219` `maxlength="300"` | ✅ 前端对；**卡在后端 255** → 见 P0-2 |
| 附件2 正式队员名单 **17 个栏目，含「长号」**（附件2） | `PersonTable.vue:124-141` 17 个 `el-option`，**逐字逐序一致** | ✅ 前端对；**卡在后端漏项** → 见 P0-4 |
| 管乐团应包含**长号**（三·（二）） | 前端下拉可选「长号」 | ✅ |

### 6.2 前端本项目对 dist 的**纠正**（dist 与红头文件不符，均已按文件改回）

列出来是为了让后端知道**这些不是前端写错，是照着红头文件改的**，别照 dist 回退：

| 红头文件原文 | dist 的错误行为 | 本项目现状 |
|---|---|---|
| 「视频格式为 MP4 或 MOV」 | `beforeUpload` 判据写成 `file.type === 'video/mov'`——该 MIME **不存在**，浏览器对 `.mov` 上报的是 `video/quicktime`，导致 **MOV 视频被无条件拒绝** | 改为 `video/quicktime`，MOV 可正常上传 |
| 「管乐团、铜管乐团现场展示曲目均为指定曲目和自选曲目各一首」 | 5e02 模块把「指定曲目」包在 `"管乐团"===form.establishment` 的三元里，**选中「铜管乐团」时不渲染该输入框**，而该字段是 `required` | 四个变体一律无条件渲染 |
| 「铜管乐团分为小学组和中学组」（无大学组） | city 编辑页（30d4）的 `groupOptions` 是 `['小学组','中学组','大学组']`，**可以选出「铜管乐团 + 大学组」** | 收敛为 `['小学组','中学组']`，与 city 新增页一致 |
| 「乐团集体电子照……JPEG 或 TIFF」 | 提示文案拼成 `JEPG`，且尾部有一个孤立右括号 | 订正为「JPEG 或 TIFF」，删去多余括号 |

### 6.3 仍需后端处理（本节与前面各条的对应关系）

| 红头文件要求 | 卡在哪 | 去向 |
|---|---|---|
| 乐团简介 300 字以内 | 后端 `_string` 默认上限 255 → 400 | **P0-2** |
| 附件2 名单含「长号」（17 栏） | 后端 `FORM_INSTRUMENTS` 16 栏、无长号 | **P0-4** |
| ~~每所学校限报一支队伍~~ → **每所学校每个组别各一支** | 后端无任何约束，草稿路径连 8 队限制也绕过 | **P0-3**（口径已于 2026-09-23 放宽，见该节【口径变更】） |
| 用餐预约 4 格 + **写明就餐人数** | 前端无控件；后端 6 格且只画 `√` | **P1-4** → 契约见 `docs/用餐预约接口契约-第十二届.md` |

### 6.4 一处**需要产品确认**的收紧（不是缺陷，但请知悉）

红头文件写「**乐团名单报名确定后不得更改**」（三·（二））。
本项目据此把列表页的「编辑」按钮**只保留在 `status === -1`（已驳回）**：

```vue
<!-- src/components/elementary/ReportList.vue:135-140 -->
<template v-if="row.status === -1">   <!-- 已驳回：查看 + 编辑 + 删除 -->
<template v-else-if="row.status === 0"> <!-- 待审核：查看 + 删除，**无编辑** -->
```

**dist 原行为是「待审核（0）也可以编辑」**，本轮把它去掉了。两处依据：
（a）暂存规范 §十三「只有驳回状态才允许修改报名」；（b）后端对非驳回状态直接回
`REPORT_NOT_REJECTED`，留着按钮点进去必然报错。

**但请产品确认这个方向**：红头文件的「确定后不得更改」最严的解释是**组委会通过（status 1）之后**才锁定；
待审核期是否算「确定」，文件没有明说。收紧之后，**学校提交后若立刻发现名单有笔误，
自己改不了**，只能等组委会驳回——如果组委会没注意到，错误就被锁进那张盖章的报名表里了。
若产品认为待审核期应当允许修改，需要后端先放开 `REPORT_NOT_REJECTED` 的判据，前端再恢复按钮。

---

## 附：一处会误导后端的过时描述

`BACKEND_ISSUES.md` §十「三条必须后端确认的点」的第 1 条写着：

> **路径单复数**：本清单按规范原文写 —— 草稿用单数 `/report/drafts`，
> 驳回编辑用复数 `/reports/{id}/edit-draft`。而项目现状**全是单数** `/report/...`。

**这段已经过期，请勿据此修改**。已用真实后端实测，现状是：

- 草稿：`/api/{scope}/report/drafts`（**复数**）
- 驳回编辑：`/api/{scope}/reports/{report_id}/edit-draft`（**复数**）

前端 `src/api/reportDraft.js` 的 `PATHS` 与后端 `views.py:765` / `:869` 逐字匹配，
**当前是通的**。照那段描述去改反而会改坏。

---

## 附：前端探针的位置与跑法

探针放在 `yl-frontend/__debug__/`（已 gitignore），**不在后端仓库里留任何文件**：

```bash
cd /d/zhuomian/yilinbei/yl-frontend
bash __debug__/run-person-fields-probe.sh
bash __debug__/run-form-fields-probe.sh
bash __debug__/run-reattach-probe.sh
bash __debug__/run-error-probe.sh
bash __debug__/run-contract-probe.sh      # 前端真实 payload → 后端真实校验
```

后端侧探针在 `%TEMP%\ylb-probe\`，跑法见本文档开头的命令。
