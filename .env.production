# 生产环境变量
#
# 【部署前缀】
# 不要在此定义部署前缀。前缀唯一来源是 vite.config.js 的 base（production 下为 /ylbxt/），
# 代码中统一读取 import.meta.env.BASE_URL。

# API 基础地址
#
# 【值的含义】这里填的是「/api 之前的那一段前缀」，请求时会自动拼上 /api/...。
#
# 【部署时二选一】
#   A) 前后端同源部署（nginx 把 /api/ 反代到后端）→ 保持本行为空即可，
#      请求会走 /api/... 打到当前域名。
#   B) 后端在另一个域名 → 填完整前缀，如 http://<后端域名>；
#      若后端挂在反向代理的子路径下，则连子路径一起填，如 http://<后端域名>/ylbxt。
#      不要带结尾斜杠（带上会拼出 //api/... 导致 404）。
#
# 【注意】不要填内网穿透域名（natapp 等）——隧道重启即换域名，
# 写进生产构建会导致线上整体不可用。
VITE_API_BASE_URL=

# 标题
VITE_APP_TITLE=「意林杯」管乐展示活动报名系统
